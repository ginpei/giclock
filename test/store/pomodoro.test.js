import { expect } from 'chai'
import { mutations } from '../../store/pomodoro.js'

describe('pomodoro', () => {
  describe('mutations', () => {
    it('is OK', () => {
      expect(true).to.eql(true)
    })
  })
})
